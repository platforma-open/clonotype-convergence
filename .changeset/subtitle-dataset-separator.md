---
'@platforma-open/milaboratories.clonotype-convergence.model': patch
---

Separate the dataset name from the settings in the page subtitle with " - " so
the commas inside the settings part (threshold, nMin, cluster filter) don't blur
into the dataset name.
