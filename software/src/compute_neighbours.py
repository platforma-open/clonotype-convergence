# Stage 1 — compute multiplicity-weighted Hamming-1 aa CDR3 neighbour count.
# Vendored from statbiophys/STAR all_class/get_df.py (no LICENSE upstream;
# licensing to be resolved with maintainers before publish — see project spec).
#
# Phase 2 fills in the actual logic. This stub keeps the entrypoint callable
# so the software pack builds.

import sys


def main() -> int:
    print("compute-neighbours stub — phase 2 not yet implemented", file=sys.stderr)
    return 1


if __name__ == "__main__":
    sys.exit(main())
